# ZInteger

Program to compute _(quickly)_ the max size of `u65535` in zig. Output is a number in decimal.

# Build Instructions

- `zig build`

## Dependencies

- [zig](https://ziglang.org/) >= 0.14.0

# Run Instructions

- `zig build run`

#